#include <bits/stdc++.h>using namespace std; int main() {    int t;    cin >> t;     while(t--) {        int n;        string s;        cin >> n;        cin >> s;         char mx = 'a';        for(int i = 0; i < n; i++) {            if(s[i] > mx) {                mx = s[i];            }        }         int size = mx - 'a' + 1;        cout << size << endl;   // newline after each test case    }}