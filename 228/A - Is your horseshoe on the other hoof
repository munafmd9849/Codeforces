#include <bits/stdc++.h>
using namespace std;
 
int main() {
    int a, b, c, d;
    cin >> a >> b >> c >> d;
 
    int count = 1;
 
    if(b != a) count++;
    if(c != a && c != b) count++;
    if(d != a && d != b && d != c) count++;
 
    cout << 4 - count;
}